export declare namespace TaskModel {
  interface TaskInfo {
    title: string;
    description: string;
    dueDate: string;
    startTime: string;
    endTime: string;
    uids: string[];
    color?: string;
    fileUrls: string[];
  }
}
